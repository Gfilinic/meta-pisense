<?xml version="1.0" ?>
<!-- This file contains the block map for an image file, which is basically
     a list of useful (mapped) block numbers in the image file. In other words,
     it lists only those blocks which contain data (boot sector, partition
     table, file-system metadata, files, directories, extents, etc). These
     blocks have to be copied to the target device. The other blocks do not
     contain any useful data and do not have to be copied to the target
     device.

     The block map an optimization which allows to copy or flash the image to
     the image quicker than copying of flashing the entire image. This is
     because with bmap less data is copied: <MappedBlocksCount> blocks instead
     of <BlocksCount> blocks.

     Besides the machine-readable data, this file contains useful commentaries
     which contain human-readable information like image size, percentage of
     mapped data, etc.

     The 'version' attribute is the block map file format version in the
     'major.minor' format. The version major number is increased whenever an
     incompatible block map format change is made. The minor number changes
     in case of minor backward-compatible changes. -->

<bmap version="2.0">
    <!-- Image size in bytes: 115.0 MiB -->
    <ImageSize> 120584192 </ImageSize>

    <!-- Size of a block in bytes -->
    <BlockSize> 4096 </BlockSize>

    <!-- Count of blocks in the image file -->
    <BlocksCount> 29440 </BlocksCount>

    <!-- Count of mapped blocks: 62.6 MiB or 54.4%     -->
    <MappedBlocksCount> 16013 </MappedBlocksCount>

    <!-- Type of checksum used in this file -->
    <ChecksumType> sha256 </ChecksumType>

    <!-- The checksum of this bmap file. When it is calculated, the value of
         the checksum has be zero (all ASCII "0" symbols).  -->
    <BmapFileChecksum> 6399be408abef1c772dc50bcb2be428dff2d399df839a6908ccb0bf0f17b6262 </BmapFileChecksum>

    <!-- The block map which consists of elements which may either be a
         range of blocks or a single block. The 'chksum' attribute
         (if present) is the checksum of this blocks range. -->
    <BlockMap>
        <Range chksum="0d379fb3437ba4e6bd5271885c4c9c29feefbe8efae572f855e30a1bd825a91d"> 0-2 </Range>
        <Range chksum="2f6ebaa3466acf76a11e6aed2d9187313fdc8a886b8cd1b30ee1b33ccc133466"> 1024-12767 </Range>
        <Range chksum="27bb6c3ffbe45e3db9a5c236d4111be8e1d4b249da96f74ecfb5c3486067443d"> 20480-20593 </Range>
        <Range chksum="3f2e254493e45cea499cf0da5b889d94423a816a3a8194dff0a9aa637380775a"> 20827-22528 </Range>
        <Range chksum="a499b7b6149fe8f78acf07ccad508d3a6fb7a5b541336a7cafb1bf5fe6a33d29"> 22592-23983 </Range>
        <Range chksum="1da4e180e5a18d9d31a33e7b875bd48654721c738d88e5f881e327e686b16b1a"> 24576-25600 </Range>
        <Range chksum="b7b355a4af90d10eddaaab350e27d8acbb21739ac13cf244717f71f1a652a5c2"> 26624 </Range>
        <Range chksum="bab4e6a5d6ef38877caddf543e92dc396a7538722dab6f24cee596db2427110b"> 29408-29439 </Range>
    </BlockMap>
</bmap>
